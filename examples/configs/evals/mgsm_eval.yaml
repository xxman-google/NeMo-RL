# Math evaluation Configuration
defaults: "gpqa_eval.yaml"

data:
  prompt_file: null
  dataset_name: "mgsm"

generation:
  vllm_cfg:
    max_model_len: 4096

env:
  math:
    verifier_type: "mgsm"
    metric_group_key: "lang"
